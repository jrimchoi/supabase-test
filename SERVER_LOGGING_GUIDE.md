# 📊 서버 로그 확인 가이드

## ✅ 추가된 로깅

**BusinessObject 페이지 (`src/app/admin/business-objects/page.tsx`):**

```typescript
📊 [BusinessObjects Page] 시작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 [DB Query] 시작...
✅ [DB Query] 완료: 50.23ms
   - 조회 개수: 50개
   - 평균: 1.00ms/item
📦 [Data Size] 8.45 KB
⏱️  [Total] 52.18ms
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**측정 항목:**
1. **DB Query Time** - Prisma 쿼리 실행 시간
2. **Item Count** - 조회된 항목 수
3. **Average Time** - 항목당 평균 시간
4. **Data Size** - JSON 데이터 크기
5. **Total Time** - 전체 서버 사이드 시간

---

## 🔍 Vercel에서 로그 확인

### 방법 1: Vercel Dashboard (실시간)

1. https://vercel.com/dashboard 접속
2. 프로젝트 선택
3. **"Logs"** 탭 클릭
4. 우측 상단 **"Real-time"** 선택
5. BusinessObject 페이지 방문
6. 로그 실시간 확인!

**검색:**
```
🔍 [DB Query]
📊 [BusinessObjects Page]
```

---

### 방법 2: Deployment Logs (과거 로그)

1. Vercel Dashboard → 프로젝트
2. **"Deployments"** 탭
3. 최신 배포 클릭
4. **"Logs"** 탭
5. 검색: "BusinessObjects"

---

### 방법 3: Runtime Logs (Function 로그)

**Edge Runtime 로그:**
```
Vercel Dashboard
→ 프로젝트
→ Logs
→ Filter: "Functions"
→ 검색: "[BusinessObjects]"
```

---

## 🎯 로그 분석

### 예시 1: 정상 (빠름)

```
📊 [BusinessObjects Page] 시작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 [DB Query] 시작...
✅ [DB Query] 완료: 50ms          ← 빠름!
   - 조회 개수: 50개
   - 평균: 1.00ms/item
📦 [Data Size] 8.45 KB            ← 작음!
⏱️  [Total] 52ms                  ← 빠름!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**결과:** ✅ 최적화 성공!

---

### 예시 2: 느림 (문제!)

```
📊 [BusinessObjects Page] 시작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 [DB Query] 시작...
✅ [DB Query] 완료: 700ms         ← 느림! (인덱스 없음)
   - 조회 개수: 200개
   - 평균: 3.50ms/item
📦 [Data Size] 92.34 KB           ← 큼! (data 필드 포함)
⏱️  [Total] 720ms                 ← 느림!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**문제:**
- ❌ DB 쿼리 700ms (인덱스 없음)
- ❌ 데이터 92KB (data 필드 포함)

**해결:**
1. createdAt 인덱스 추가
2. data 필드 제거
3. limit 줄이기

---

## 🔍 Prisma 쿼리 로그 (상세)

**이미 활성화됨:**
```typescript
// src/lib/prisma.ts
log: process.env.NODE_ENV === 'development' 
  ? ['query', 'error', 'warn']  // 개발 환경
  : ['error'],                   // 프로덕션
```

**Vercel 로그에서:**
```
prisma:query SELECT "BusinessObject"."id", ...
FROM "BusinessObject" 
LEFT JOIN "Type" ON ...
ORDER BY "createdAt" DESC
LIMIT 50

Duration: 50ms
```

---

## 🎯 성능 기준

### DB 쿼리 시간:
- ✅ **50ms 이하** - 최적화됨
- ⚠️ **50-200ms** - 보통 (인덱스 확인)
- ❌ **200ms+** - 느림 (인덱스 추가 필요!)

### 데이터 크기:
- ✅ **10KB 이하** - 최적화됨
- ⚠️ **10-50KB** - 보통
- ❌ **50KB+** - 큼 (필드 제거 필요!)

### 전체 시간:
- ✅ **100ms 이하** - 완벽!
- ⚠️ **100-500ms** - 보통
- ❌ **500ms+** - 느림 (최적화 필요!)

---

## 🚀 로그 확인 순서

**배포 후:**

1. **Vercel Dashboard → Logs → Real-time**
2. BusinessObject 페이지 방문
3. 로그 확인:
   ```
   🔍 [DB Query] 완료: XXms
   📦 [Data Size] XX KB
   ⏱️  [Total] XXms
   ```
4. 성능 분석
5. 필요 시 추가 최적화

---

## 📝 문제 해결

### DB 쿼리가 느리면:
```sql
-- Supabase SQL Editor에서:
CREATE INDEX IF NOT EXISTS "BusinessObject_createdAt_idx" 
  ON "BusinessObject"("createdAt" DESC);
```

### 데이터 크기가 크면:
```typescript
// data 필드 제거
select: {
  // data: true,  // ← 제거!
}
```

### 조회 개수가 많으면:
```typescript
take: 30,  // 50 → 30
```

---

## 🎨 클라이언트 렌더링 로그

**브라우저 콘솔:**
```
🎨 [BusinessObjectList] Render: XXms
```

**전체 타임라인:**
```
서버:
  📊 [BusinessObjects Page] 시작
  🔍 [DB Query] 완료: 50ms
  ⏱️  [Total] 52ms

네트워크:
  Transfer: 200ms (8KB)

클라이언트:
  🎨 [BusinessObjectList] Render: 500ms

총: 52ms + 200ms + 500ms = 752ms
```

---

완벽합니다! 이제 Vercel 로그에서 정확한 쿼리 시간을 확인할 수 있습니다! 🚀

**배포 후 확인:**
1. Vercel Dashboard → Logs → Real-time
2. BusinessObject 페이지 방문
3. "🔍 [DB Query]" 검색!


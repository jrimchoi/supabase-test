# Vercel 강제 재배포 가이드

## 🎯 현재 상황

- ✅ 코드 수정 완료 (로컬)
- ✅ 커밋 완료 (`0aaff9c`)
- ⚠️ Vercel이 이전 버전 실행 중

**Vercel 로그에서 여전히:**
```javascript
select: {
  type: true,  // ❌ 구버전 코드
  name: true,
}
```

---

## 🚀 해결 방법

### 방법 1: GitHub Desktop 또는 터미널에서 푸시

#### GitHub Desktop:
1. GitHub Desktop 앱 열기
2. 왼쪽 상단에서 `supabase-test` 저장소 선택
3. 우측 상단 **Push origin** 버튼 클릭

#### 터미널:
```bash
cd /Users/jrchoi/Documents/GitHub/supabase-test
git push
```

---

### 방법 2: Vercel Dashboard에서 수동 재배포 (가장 쉬움!)

#### 1. Vercel Dashboard 접속
```
https://vercel.com/dashboard
```

#### 2. 프로젝트 선택
- **supabase-test** 클릭

#### 3. Deployments 탭
- 상단 **Deployments** 탭 클릭

#### 4. 최신 배포 찾기
- 가장 위의 배포 (Production)
- Commit: `0aaff9c` 또는 최신 커밋

#### 5. 재배포 실행
- 배포 옆 **...** (점 3개) 메뉴 클릭
- **Redeploy** 선택
- **Use existing Build Cache** 체크 해제! ⚠️ 중요!
- **Redeploy** 버튼 클릭

---

## ⚠️ 중요: Build Cache 제거

Vercel 재배포 시:

```
Use existing Build Cache [ ] ← 체크 해제!
```

**이유:**
- 캐시를 사용하면 이전 Prisma Client 사용
- 새 스키마와 불일치 발생
- 반드시 **캐시 없이 재빌드** 필요!

---

## 📋 재배포 후 확인

### 1. 빌드 로그 확인
```
Vercel Dashboard → 배포 클릭 → Building 탭
```

**확인 사항:**
- ✅ `npm install` 실행
- ✅ `npx prisma generate` 실행 (자동)
- ✅ `next build` 성공
- ✅ TypeScript 통과

### 2. 런타임 로그 확인
```
배포 클릭 → Runtime Logs 탭
```

**확인 사항:**
- ✅ Prisma 에러 없음
- ✅ `type` 컬럼 에러 없음

### 3. 앱 접속 확인
```
https://supabase-test-r6mfhneg6-jrimchoi1s-projects.vercel.app/admin/policies/policy2
```

**확인 사항:**
- ✅ 페이지 정상 로딩
- ✅ Type 정보 표시
- ✅ 에러 없음

---

## 🔍 현재 커밋 상태

```
로컬: 0aaff9c ← 최신 (type: true 제거됨)
원격: 0aaff9c ← 동기화됨
Vercel: ??? ← 이전 버전 실행 중
```

**Vercel이 최신 커밋을 배포하지 않았거나, 캐시 문제일 가능성 높음**

---

## 🎯 권장 조치 (순서대로)

### 1단계: GitHub 푸시 확인
```bash
# VS Code 또는 GitHub Desktop에서 푸시
# 또는
git push
```

### 2단계: Vercel 재배포
```
Vercel Dashboard → Deployments → ... → Redeploy
⚠️ "Use existing Build Cache" 체크 해제!
```

### 3단계: 배포 완료 대기
```
빌드 시간: 약 2-3분
```

### 4단계: 확인
```
앱 접속 → 시크릿 모드 또는 Hard Refresh
```

---

## 💡 빠른 체크

**GitHub에 푸시되었는지 확인:**
```
https://github.com/jrimchoi/supabase-test/commits/main
```

최신 커밋이 `0aaff9c`인지 확인

**Vercel 자동 배포:**
- GitHub에 푸시하면 Vercel이 자동으로 배포 시작
- 하지만 Build Cache 때문에 문제가 있을 수 있음
- 수동 Redeploy가 더 확실함

---

## 🎊 재배포 후

정상 작동할 것입니다! 🚀

**체크리스트:**
- ✅ 최신 코드 푸시
- ✅ Vercel 재배포 (캐시 없이!)
- ✅ `/admin/policies/policy2` 접속 확인
- ✅ 에러 없음!

